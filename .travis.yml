language: php

php:
  - 7.1
  - 7.2

install:
  - composer install -vvv --profile --prefer-dist

script:
  - ./vendor/bin/phpunit -c phpunit.xml --coverage-text

notifications:
  email:
    recipients:
      - 36749952@qq.com
      on_success: always
      on_failure: always